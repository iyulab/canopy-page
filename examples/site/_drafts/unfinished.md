# Not published

This page is inside `_drafts`, which the settings file excludes. It is not in the sidebar, not in
the output, and not reachable from the published site.

It also links to [a page that does not exist](nowhere.md). That link is never reported, because
an excluded page is not part of the site — which is what makes a drafts folder usable for drafts.
